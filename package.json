{
  "name": "rachel-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter @rachel/frontend dev",
    "dev:backend": "pnpm --filter @rachel/backend dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "db:migrate": "pnpm --filter @rachel/backend db:migrate",
    "db:seed": "pnpm --filter @rachel/backend db:seed",
    "db:studio": "pnpm --filter @rachel/backend db:studio"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "@types/node": "^20.11.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}

